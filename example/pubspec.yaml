name: example
description: Demonstrates how to use the logger_flutter plugin.
publish_to: 'none'
version: 23.8.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  logger_flutter:
    path: ../

flutter:
  uses-material-design: true